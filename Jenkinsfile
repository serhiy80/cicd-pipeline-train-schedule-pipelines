pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
              git branch: 'master', url: 'git@github.com:serhiy80/cicd-pipeline-train-schedule-pipelines.git'  
            }
        }
    }
}
